name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    # Linux 完結のロジック（config / scan / guid / WiX 生成のゴールデン）をテストする
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - name: Run tests
        run: uv run pytest -q
